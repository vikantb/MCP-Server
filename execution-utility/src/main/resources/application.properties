server.port=8081

# Security API Key for authentication (fallback to test-api-key)
secure.api.key=${EXECUTION_API_KEY:test-api-key}

# Allowed command binaries (including Windows equivalents for local testing)
allowed.commands=tail,grep,cat,ls,df,free,dir,type,findstr,ipconfig,echo
